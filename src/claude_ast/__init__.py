"""claude-ast — AST-backed code navigation for Claude Code.

Precise, structural answers over a real syntax tree, with graded confidence.
The `model` package is the central contract; every other package speaks it.
"""

__version__ = "0.0.0"
